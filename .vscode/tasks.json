{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build cpp",
            "type": "shell",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${fileDirname}\\program.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}